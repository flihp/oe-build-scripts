MACHINE ?= "qemux86-64-tpm2"

DL_DIR ?= "${TOPDIR}/downloads"
SSTATE_DIR ?= "${TOPDIR}/sstate-cache"
TMPDIR ?= "${TOPDIR}/tmp"
PACKAGE_CLASSES ?= "package_ipk"
EXTRA_IMAGE_FEATURES += "debug-tweaks"
USER_CLASSES += "buildstats image-mklibs image-prelink"
PATCHRESOLVE ?= "noop"
CONF_VERSION = "1"
